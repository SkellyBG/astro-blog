import type { SiteConfig } from './types';

export const siteConfig: SiteConfig = {
    title: 'My Astro Site',
    description: 'A minimal Astro site with TypeScript support',
    lang: 'en-US',
    ogLocale: 'en_US',
    themeColor: '#3c366b',
    author: 'Your Name',
}; 